apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nuhiris

resources:
  - namespace.yaml
  - api-deployment.yaml
  - web-deployment.yaml
  - postgres-statefulset.yaml
  - redis-deployment.yaml
  - keycloak-deployment.yaml
  - ingress.yaml
  - configmaps.yaml
  - network-policy.yaml

commonLabels:
  app.kubernetes.io/part-of: nuhiris
